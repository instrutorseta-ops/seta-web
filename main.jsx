import React from "react";
import ReactDOM from "react-dom/client";
import SetaApp from "./SetaApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SetaApp />
  </React.StrictMode>
);
